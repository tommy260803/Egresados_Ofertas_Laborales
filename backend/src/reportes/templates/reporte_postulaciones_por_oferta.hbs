<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <title>{{titulo}}</title>
  <style>
    @page {
      margin: 1.5cm;
    }
    body {
      font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
      color: #1F2937;
      line-height: 1.5;
      margin: 0;
      padding: 0;
      background-color: #ffffff;
    }
    .header {
      border-bottom: 2px solid #1E3A8A;
      padding-bottom: 15px;
      margin-bottom: 20px;
      display: table;
      width: 100%;
    }
    .header-left {
      display: table-cell;
      vertical-align: bottom;
    }
    .header-right {
      display: table-cell;
      vertical-align: bottom;
      text-align: right;
      font-size: 10px;
      color: #6B7280;
    }
    .system-label {
      color: #1E3A8A;
      font-size: 11px;
      font-weight: 700;
      text-transform: uppercase;
      margin: 0;
    }
    h1 {
      color: #111827;
      font-size: 24px;
      margin: 2px 0 0 0;
      font-weight: 800;
    }
    .report-meta {
      background-color: #F9FAFB;
      border: 1px solid #E5E7EB;
      border-radius: 4px;
      padding: 10px 15px;
      margin-bottom: 20px;
      font-size: 11px;
    }
    .meta-grid {
      display: table;
      width: 100%;
    }
    .meta-item {
      display: table-cell;
    }
    .meta-label {
      font-weight: 700;
      color: #4B5563;
      margin-right: 5px;
    }
    table.data-table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
    }
    table.data-table th {
      background-color: #F3F4F6;
      color: #374151;
      font-weight: 700;
      text-align: left;
      padding: 10px;
      font-size: 11px;
      border-bottom: 2px solid #E5E7EB;
      text-transform: uppercase;
    }
    table.data-table td {
      padding: 8px 10px;
      font-size: 11px;
      border-bottom: 1px solid #E5E7EB;
      color: #4B5563;
    }
    table.data-table tr:nth-child(even) {
      background-color: #F9FAFB;
    }
    .no-data {
      text-align: center;
      padding: 40px;
      background-color: #F9FAFB;
      border: 1px dashed #D1D5DB;
      border-radius: 4px;
      color: #6B7280;
      font-size: 13px;
    }
    .footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 9px;
      color: #9CA3AF;
      border-top: 1px solid #E5E7EB;
      padding-top: 5px;
    }
    .badge-status {
      display: inline-block;
      padding: 2px 6px;
      border-radius: 4px;
      font-size: 10px;
      font-weight: 700;
      background-color: #F3F4F6;
      color: #374151;
      text-transform: uppercase;
    }
  </style>
</head>
<body>
  <div class="header">
    <div class="header-left">
      <p class="system-label">Gestión de Candidatos</p>
      <h1>{{titulo}}</h1>
    </div>
    <div class="header-right">
      <div>Generado: {{generatedAt}}</div>
      <div>Reporte Administrativo</div>
    </div>
  </div>

  <div class="report-meta">
    <div class="meta-grid">
      <div class="meta-item"><span class="meta-label">ID Oferta:</span> #{{filtros.ofertaId}}</div>
      <div class="meta-item"><span class="meta-label">Desde:</span> {{filtros.fechaDesde}}</div>
      <div class="meta-item"><span class="meta-label">Hasta:</span> {{filtros.fechaHasta}}</div>
    </div>
  </div>

  {{#if rows.length}}
    <table class="data-table">
      <thead>
        <tr>
          <th>Oferta</th>
          <th>Egresado</th>
          <th>Email</th>
          <th>Estado</th>
          <th>Fecha</th>
        </tr>
      </thead>
      <tbody>
        {{#each rows}}
        <tr>
          <td><strong>{{this.oferta_titulo}}</strong></td>
          <td>{{this.egresado}}</td>
          <td>{{this.email}}</td>
          <td><span class="badge-status">{{this.estado_actual}}</span></td>
          <td>{{this.fecha_postulacion}}</td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <div class="no-data">
      No se encontraron postulaciones registradas para los filtros seleccionados.
    </div>
  {{/if}}

  <div class="footer">
    Sistema de Seguimiento de Egresados &copy; 2024 | Informe de Candidatos
  </div>
</body>
</html>
